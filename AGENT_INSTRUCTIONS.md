# Agent Instructions: 納品書・請求書自動化システム

このドキュメントは、このシステムを扱うAIエージェント向けの指示書です。
システムを維持、実行、または拡張する際に参考にしてください。

## システムの役割
CSVデータから、Jinja2テンプレートを使用してHTMLを生成し、PlaywrightでPDF（請求書・納品書）に変換する自動化ツールです。

## ファイル構成
- `generate_pdf.py`: メインスクリプト。
- `invoice_template.html`: 請求書テンプレート。
- `delivery_template.html`: 納品書テンプレート。
- `data.csv`: 処理対象データ（会社名, 品目, 金額, ステータス）。
- `納品書・請求書/`: 出力先フォルダ。

## エージェントへの具体的指示

### 1. セットアップ
新しい環境で実行する場合、以下の手順を踏んでください。
- Pythonパッケージのインストール: `pip install jinja2 playwright`
- Playwrightブラウザのインストール: `playwright install chromium`

### 2. PDFの生成
- `data.csv` の「ステータス」列が「PDF作成済」でない行が処理対象になります。
- 実行コマンド: `python generate_pdf.py`
- 実行後、`data.csv` の「ステータス」が自動的に「PDF作成済」に更新されることを確認してください。

### 3. 内容の変更要請への対応
- **発行者情報の変更**: `generate_pdf.py` 内の `issuer_info` 辞書を編集します。
- **デザインの変更**: 各 `.html` ファイルの CSS 部分（`<style>` タグ内）を編集します。
- **データ項目（列）の追加**: CSVの列、スクリプトのデータ抽出、HTMLのプレースホルダ（`{{ ... }}`）の3箇所を同時に更新する必要があります。

### 4. トラブルシューティング
- **文字化け・豆腐化**: テンプレート内で `Google Fonts (Noto Sans JP)` を読み込んでいますが、環境によって日本語フォントが必要な場合があります。
- **PDFが生成されない**: ブラウザ（Chromium）が正しくインストールされているか、`playwright install` を再実行してください。
